# AegisIR v2.0 重构设计文档

日期：2026-08-24 · 状态：已确认（用户授权按此实施）

## 1. 问题定义（需求理解）

用户反馈三类问题：

| 反馈 | 根因分析 |
|---|---|
| "扫描扫不出来主机" | 当前探测完全依赖 scapy 原始套接字，而 Npcap 默认仅允许管理员收发包；值守人员在普通权限/不熟悉提权时扫描必然全部落空 |
| "UI 布局逻辑太难看" | 单屏三栏塞下全部功能，视觉层级弱、操作动线混乱（探测→选目标→隔离分散在不同区域） |
| "操作逻辑/更多网络环境/手动选网卡" | 多网卡机器（WiFi+有线+WSL+蓝牙虚拟网卡共 14 个接口）无法选择探测出口；引擎无降级路径；跨网段指引埋在报错里 |

## 2. 调研结论（已在本机验证）

1. `ping.exe` 无需管理员，0.1s/台，回包可靠（rc=0 / TTL=）
2. ping 之后系统 ARP 表立刻出现目标表项 → **免权限获得 MAC**
3. `arp -a` 的存量表项本身就是"被动发现"数据源（零流量、零权限）
4. `nbtstat -A <ip>`（Windows）免权限取 NetBIOS 主机名
5. socket connect 探测对本网络无效（防火墙 drop），仅作辅助
6. scapy 网卡对象含友好名（name/description）+ IP + MAC，可做网卡选择器

## 3. 核心设计

### 3.1 双探测引擎（自动降级）

```
discover(engine=auto)
  ├─ raw 引擎（管理员 + Npcap）：ARP sweep + ICMP + TCP SYN + UDP（现有 scapy 实现）
  └─ compat 引擎（任何权限）：
       ① 被动快照 arp -a（0 秒出部分结果）
       ② 64 线程系统 ping 全网段（~3s/254 台）
       ③ ping 后再读 arp -a 差分 → MAC
       ④ TCP connect 辅助（抓禁 ping 且有开放端口的主机）
       ⑤ DNS + nbtstat 富化主机名
  auto 规则：admin && pcap_ok → raw，否则 compat；手动强制任一
```

兼容引擎让工具在**无管理员、受限终端、锁死环境**都能用——这是"更多网络环境"的核心。GUI 顶部显示当前引擎徽章。

### 3.2 网卡选择

- `list_interfaces()`：过滤有 IP 的接口，返回 {id(GUID), 名称, IP, MAC, 是否默认}
- 每网卡附直连网段 + 该网卡网关（从路由表推导）
- GUI 探测栏网卡下拉框：切换自动填充该网卡网段；扫描/隔离均绑定所选网卡
- raw 引擎 srp 绑定 iface；compat 引擎按系统路由（选择主要用于网段预填与隔离绑定）

### 3.3 隔离按网卡上下文

- prepare_isolation 接受 iface：网关取该网卡网关（无则默认网关）；Isolator 绑定 iface 收发

### 3.4 UI 重设计（侧边栏多视图）

```
┌──────────────────────────────────────────────────────┐
│ AegisIR  [节点▾]        ●引擎:完整 ●管理员 ●网关 10.x  │
├──────────┬───────────────────────────────────────────┤
│ ◉ 探测    │  视图1 探测：网卡/网段/深度/开始 + 结果大表  │
│          │  视图2 隔离：活动隔离大卡(倒计时/恢复)        │
│ ◉ 隔离 ② │  视图3 历史：会话表 + 审计时间线             │
│          │  视图4 节点：环境详情/节点管理/部署指引       │
│ ◉ 历史    │                                           │
│ ◉ 节点    │                                           │
└──────────┴───────────────────────────────────────────┘
```

- 深空导航蓝配色、左侧 220px 图标导航（活动隔离数角标）
- 结果表：IP 徽标（网关🔒/本机🔒/跨网段）、命中手段 chips、开放端口、行内隔离按钮
- 隔离确认弹窗：目标卡 + 可达性预检（/api/probe）+ 模式卡片 + 自动恢复时长 + 演练
- 空状态/加载态/错误横幅全部设计

### 3.5 操作动线

扫描 → 表格行点【隔离】→ 弹窗（可达性预检自动跑）→ 确认 → 自动切到隔离视图看实时状态 → 一键恢复。目标不可达时弹窗直接提示。跨网段目标弹窗给出节点部署一条龙指引（含可复制命令）。

## 4. API 变更

| 接口 | 变更 |
|---|---|
| GET /api/interfaces | 新增：网卡列表（含网段/网关/默认标记） |
| POST /api/scan | 增加 iface / engine 参数 |
| POST /api/isolate | 增加 iface 参数 |
| POST /api/probe | 新增：单目标可达性预检（ping+tcp，免权限） |
| GET /api/doctor | 增加 engine 字段（raw 可用性 + 当前模式） |

## 5. 不做什么（YAGNI）

- 不做 RST 注入 / DHCP 攻击 / 远程 ARP（已论证不可靠或不可接受）
- 不做多控制台服务器级联同步（节点切换已够用）
- 不引入前端框架/构建链（保持零依赖单文件可离线部署）

## 6. 验收标准

1. 无管理员终端：`scan` 能真实发现本网段主机（含 MAC/厂商/主机名）
2. GUI 四视图可用，网卡下拉能切换并影响扫描/隔离
3. 单元测试（免权限可跑）全绿
4. exe 重新打包后功能一致
