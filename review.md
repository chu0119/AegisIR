# AegisIR 评审报告

日期：2026-08-24 · v2.0 深度重构 + v2.1 能力增强（对照 design.md / plan.md）

## v2.1 增强交付对照

| 设计项 | 状态 | 验证方式 |
|---|---|---|
| 部署命令一键复制（token 自动生成） | ✅ | 浏览器实测：`AegisIR.exe gui --listen any --token <18位hex>` 一键复制 + 换令牌 |
| 探测手段可选（引擎/手段/被动） | ✅ | GUI 勾选零流量被动 + IP 范围实测命中网关；CLI `--methods passive/arp,tcp` |
| IP 范围扫描 | ✅ | 实测 `10.113.32.1-10.113.32.20` 被动扫描出表 |
| 隔离生效验证（ARP 广播嗅探） | ✅ | 代码路径 + 快照字段（arp_requests/verified）；实际生效需管理员环境（嗅探依赖 raw） |
| 恢复自动回探 | ✅ | /api/restore 返回 online 字段，前端区分在线/防火墙挡 ping |
| 批量隔离 / 筛选 / CSV 导出 / 时长预设 / island 保护名单 | ✅ | DOM 验证全部就位；批量路径复用单目标 API |
| 添加节点表单化 | ✅ | 三字段弹窗（地址/令牌/备注）+ 测试并接入 |
| 单元测试 | ✅ | 23/23 通过（新增范围解析/token/被动模式 6 项） |
| exe | ✅ | v2.1 重新打包，UAC 清单确认 |

## v2.0 交付对照（摘要）

双探测引擎自动降级（compat 免权限实测发现网关+MAC+厂商）、被动 ARP 快照零流量发现、网卡手动选择全链路、可达性预检、四视图 UI 重设计、17 项单测全绿——详见 Git 历史与本文件先前版本。

## 修复的缺陷（过程中发现）

1. **Critical**：compat 引擎上线前，非管理员环境扫描必然 0 结果（根因：Npcap 仅管理员可收发包）——双引擎彻底解决
2. **Major**：v1 前端轮询计数器不自增导致 doctor/网卡状态永不刷新（重写时引入，DOM 验证发现并修复）
3. **Minor**：ARP 表解析正则不兼容 Linux `ether` 格式（单测发现并修复）
4. **Minor**：/20 大网段整段预填会触发扫描上限拒绝（预填策略改为 > /22 自动缩为本机 /24）
5. **Minor**：可达性预检串行最慢 3.5s（改为 ping/TCP 并行，约 1s）

## 已知限制（记录在案，非缺陷）

- TCP connect 探测对全 drop 防火墙主机无效（无 RST 回包），此场景依赖管理员 raw 引擎
- compat 引擎的 MAC 依赖系统 ARP 表，跨网段目标无 MAC（正常，隔离本就要求同网段）
- nbtstat 仅 Windows；Linux 下 NetBIOS 富化自动跳过
- 服务进程曾出现一次无 traceback 的外部终止（疑似运行环境回收），复现同一负载未能复现，服务带 faulthandler 继续观察

## 结论

v2.0 设计目标全部达成，测试全绿，可交付使用。遗留观察项仅一条（进程外部终止原因待环境侧确认），不影响功能。
